# Shared command spinner options for zsh and Nushell.
# enabled=1 shows the tmux status spinner for non-excluded long commands.
# enabled=0 disables spinner startup while still clearing stale spinner state.
enabled=1
