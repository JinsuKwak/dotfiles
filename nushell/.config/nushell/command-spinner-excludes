# One pattern per line.
# Exact entries match either the first command word or the full command line.
# Entries ending in * are treated as command-line prefixes.
# Keep long-running servers, full-screen TUIs, shells, and session commands here.

nvim
vim
vi
yazi
lazygit
less
more
man
ssh
tmux
top
htop
btop
watch
tail -f*
tail -F*
npm run dev*
pnpm dev*
yarn dev*
bun dev*
python -m http.server*
